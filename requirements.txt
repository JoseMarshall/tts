# Web server
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
numpy>=1.26

# --- Model backend (install on a machine with a CUDA GPU) ---
# These are only required when TTS_BACKEND=qwen. The default "mock"
# backend needs none of them, so the server runs anywhere for testing.
#
#   pip install -U qwen-tts torch soundfile
#
# torch>=2.3            # match your CUDA version
# qwen-tts>=0.1
# soundfile>=0.12       # only needed to decode uploaded reference audio
